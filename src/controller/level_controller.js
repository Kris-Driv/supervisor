const LevelController = {

    supervisor: null,

    _boot(supervisor) {
        LevelController.supervisor = supervisor;
    },

    setup: () => {
        
    },

};

module.exports = LevelController;